// Import modules
import React from 'react';
import { StatusBar } from 'react-native';

// CustomStatusBar() sets the style of the device status bar (default or dark theme)
const CustomStatusBar = () => {
    return (
        <StatusBar translucent backgroundColor='#5968F0' />
    );
}

export default CustomStatusBar;